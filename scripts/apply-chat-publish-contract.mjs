import { readFile, writeFile } from 'node:fs/promises';

const replaceExactlyOnce = async (path, oldText, newText) => {
  const before = await readFile(path, 'utf8');
  const first = before.indexOf(oldText);
  if (first < 0) throw new Error(`Expected text not found in ${path}`);
  if (before.indexOf(oldText, first + oldText.length) >= 0) {
    throw new Error(`Expected text occurs more than once in ${path}`);
  }
  const after = before.slice(0, first) + newText + before.slice(first + oldText.length);
  await writeFile(path, after);
};

const definitionsPath = 'packages/core/server/lib/mcp-tool-definitions-2.ts';
const oldCreateDescription = `      'Create a CMS object from object_type, site, and the per-type body. requested_id is optional — omit it to have a valid id minted server-side. For recipe types (template / section_template / theme): REUSE FIRST — object_inventory lists existing recipes with self-describing summaries; create only when none fits, and include description/whenToUse/scope (required to publish). Creation may be restricted per type by the committed creation policy — check object_contract(<type>).creation_policy; a denial is a 403 with code creation_restricted. content_item (articles) is NOT created here from admin chat (ART-1) — a new article is produced by the publishing workflow (run_workspace_workflow → check_workspace_run_readiness → publish_workspace_run → release_workspace_run), which is what builds the sourcing/claim/compliance record ART-2 requires to publish and applies the aggression ceiling. The chat registry refuses object_create for content_item and tells you this; to revise an article that already exists use object_checkout + object_patch. Committed legacy posts stay on the old article tools. tracking_config (W13) is the per-site tracker-registry SINGLETON: creation is human/seed-only and a second active registry is refused (409) — read object_contract("tracking_config") and edit the existing trk_* object instead. If this call itself times out or 502s (ambiguous whether the object was created), retry with the SAME idempotency_key to get back the original created object instead of creating a duplicate — this matters most when requested_id is omitted, since a fresh id is minted on every genuinely new call.',`;
const newCreateDescription = `      'Create a CMS object from object_type, site, and the per-type body. requested_id is optional — omit it to have a valid id minted server-side. For recipe types (template / section_template / theme): REUSE FIRST — object_inventory lists existing recipes with self-describing summaries; create only when none fits, and include description/whenToUse/scope (required to publish). Creation may be restricted per type by the committed creation policy — check object_contract(<type>).creation_policy; a denial is a 403 with code creation_restricted. content_item (articles) has two supported creation paths: internal admin chat (ART-1) uses run_workspace_workflow → check_workspace_run_readiness → publish_workspace_run → release_workspace_run; authorized external publishing-plugin/MCP clients (including regular chats, custom GPTs and agents) may create directly with object_validate(candidate body) → object_create → object_checkout → object_publish → object_checkin → release_to_production. The direct MCP/chat path does not produce the CMS-Agent workspace readiness/judgement record and does not server-enforce the aggression ceiling, so callers must read and obey object_contract("content_item").aggression_ceiling. To revise an article that already exists use object_checkout + object_patch. Committed legacy posts stay on the old article tools. tracking_config (W13) is the per-site tracker-registry SINGLETON: creation is human/seed-only and a second active registry is refused (409) — read object_contract("tracking_config") and edit the existing trk_* object instead. If this call itself times out or 502s (ambiguous whether the object was created), retry with the SAME idempotency_key to get back the original created object instead of creating a duplicate — this matters most when requested_id is omitted, since a fresh id is minted on every genuinely new call.',`;
await replaceExactlyOnce(definitionsPath, oldCreateDescription, newCreateDescription);

const contractPath = 'packages/core/lib/registry/object-contract.ts';
const oldArticleWorkflow = `    // ART-1: articles have ONE production path. The sequence says so FIRST,\n    // before the generic create step, because the contract is the only place\n    // an agent reliably reads before writing.\n    ...(objectType === 'content_item'\n      ? [\n          'START PRODUCTION, DO NOT HAND-BUILD: a NEW article is produced by the publishing workflow — run_workspace_workflow (the editor’s brief verbatim as input.instructions) → check_workspace_run_readiness → publish_workspace_run → release_workspace_run. The workflow is what produces the sourcing, claim, compliance and score record ART-2 requires to publish, and the aggression ceiling is enforced only on that path. A direct object_create of a content_item is REFUSED in admin chat; the steps below apply to an article that already exists.',\n        ]\n      : []),`;
const newArticleWorkflow = `    // ART-1 is an admin-chat rule, not a tenant MCP rule. Say that FIRST so\n    // a publishing plugin/chat never reaches for CMS-Agent-only workflow tools\n    // that are deliberately absent from its plane.\n    ...(objectType === 'content_item'\n      ? [\n          'NEW ARTICLE — choose the available plane. CMS-Agent/admin-chat plane: run_workspace_workflow (the editor’s brief verbatim as input.instructions) → check_workspace_run_readiness → publish_workspace_run → release_workspace_run; this produces the workspace readiness/judgement record and applies the aggression ceiling. External MCP/chat plane (regular chat, custom GPTs and agents): object_validate(candidate body) → object_create → object_checkout → object_publish → object_checkin → release_to_production. The direct path does not produce the CMS-Agent workspace readiness/judgement record and the aggression ceiling is not server-enforced there, so read and obey this contract’s aggression_ceiling. Direct object_create(content_item) is refused only in the internal admin-chat registry, not on an authorized MCP publishing plane.',\n        ]\n      : []),`;
await replaceExactlyOnce(contractPath, oldArticleWorkflow, newArticleWorkflow);

const oldCreateStep = `    objectType === 'content_item'\n      ? 'object_create (omit requested_id to mint one) — operator/workflow path only; refused in admin chat, see the production step above'\n      : 'object_create (omit requested_id to mint one) — for a new object',`;
const newCreateStep = `    objectType === 'content_item'\n      ? 'object_create (omit requested_id to mint one) — create a new content_item on the external MCP/chat plane; internal admin chat is refused and must use the CMS-Agent workflow'\n      : 'object_create (omit requested_id to mint one) — for a new object',`;
await replaceExactlyOnce(contractPath, oldCreateStep, newCreateStep);

const testPath = 'tests/netlify/chat-publishing-contract.test.ts';
const testSource = `import '../../sites/drlurie/config/policy-bindings.js';\nimport assert from 'node:assert/strict';\nimport test from 'node:test';\n\nimport { buildObjectContract } from '../../packages/core/lib/registry/object-contract.js';\nimport { TOOL_DEFINITIONS_PART2 } from '../../packages/core/server/lib/mcp-tool-definitions-2.js';\n\ntest('authorized external chat clients are told to use the direct content_item publishing path', () => {\n  const createTool = TOOL_DEFINITIONS_PART2.find((tool) => tool.name === 'object_create');\n  assert.ok(createTool, 'object_create must stay exposed');\n  assert.match(createTool.description, /two supported creation paths/i);\n  assert.match(createTool.description, /authorized external publishing-plugin\\/MCP clients/i);\n  assert.match(createTool.description, /object_validate\\(candidate body\\).*object_create.*object_publish.*release_to_production/i);\n\n  const sequence = buildObjectContract('content_item').workflow.sequence;\n  assert.match(sequence[0], /CMS-Agent\\/admin-chat plane/i);\n  assert.match(sequence[0], /External MCP\\/chat plane/i);\n  assert.match(sequence[0], /Direct object_create\\(content_item\\) is refused only in the internal admin-chat registry/i);\n  assert.ok(\n    sequence.some(\n      (step) => step.startsWith('object_create (') && step.includes('external MCP/chat plane')\n    ),\n    'content_item workflow must name direct object_create for the external chat plane'\n  );\n  assert.doesNotMatch(sequence[0], /DO NOT HAND-BUILD/);\n});\n`;
await writeFile(testPath, testSource);

console.log('Applied direct chat publishing contract patch.');
