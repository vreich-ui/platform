---
import '@site/config/policy-bindings';
import { getSiteIdentity } from '@core/lib/site-identity';
import AdminLayout from '~/layouts/AdminLayout.astro';
import VisualIdentityWorkspace from '~/admin/VisualIdentityWorkspace';
const identity = getSiteIdentity();
---

<AdminLayout title="Admin — Visual identity">
  <VisualIdentityWorkspace client:load identity={identity} />
</AdminLayout>
