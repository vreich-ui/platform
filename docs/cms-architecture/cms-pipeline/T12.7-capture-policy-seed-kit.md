# T12.7 — Policy plumbing + seed kit: one rulebook, committed templates, a runbook

**Phase 12 · depends_on: T12.1–T12.5 merged (R-C2 v2) · mode: auto · model: Opus 4.8 · effort: medium**
**Blocks:** T12.8, T12.9, T12.11.

## Read first

`decisions/2026-08-13-capture-productization-rulings.md` (**R-C2 v2 governs**),
CMS-Agent repo `src/agent/projects/projectTypes.ts` (`ProjectCapturePolicy` —
the authoritative shape) + `projectAdmin.ts` (`capturePolicySchema`) + the live
Zilberman example in `src/agent/projects/platform/definition.ts`,
`packages/core/cli/capture/snapshot-v1.mjs` (`validateCapturePolicy` — what the
engine reads today), `emit.mjs` (`capturePolicyFromProject`), `score.mjs`
(fidelity override reading).

## Context

The rulebook exists — CMS-Agent's `ProjectCapturePolicy`, deny-all by default,
Zilberman already configured. What does NOT exist: a committed statement that
this is the ONE shape (the engine's `--policy` JSON grew up separately), any
committed example a human can copy, and a runbook. This task makes the
CMS-Agent shape canonical and the engine a consumer of it.

## Scope — files to create/modify (platform repo)

- Align `validateCapturePolicy` + `capturePolicyFromProject` + `score.mjs`'s
  fidelity reading to accept the `ProjectCapturePolicy` shape verbatim (field
  names included: `allowedCrawlOrigins`, `allowedPathPrefixes`, `rights`,
  `designReferences`, `fidelity.coverageRubricOverride`, …). One shape, every
  entry point; the CLI `--policy <file>` flag takes that same JSON. Keep a
  compatibility test proving the committed Zilberman fixture policy round-trips.
- ✚ `packages/core/cli/capture/fixtures/capture-policy.template.json` — a
  fully-commented example (as JSON + a doc table) with the deny-all default
  stated and every field explained in one line.
- ✚ `docs/cms-architecture/capture-runbook.md`: seed a target (via CMS-Agent
  `project.update` with `capturePolicy`, or the JSON file for CLI runs) → run
  the five stages → read the reports. Concrete commands, the Zilberman worked
  example, and the "what the numbers mean" cheat sheet (coverage, gaps,
  verdict).
- Document the deferred client-store consent object (R-C2 v2) in the runbook's
  "future" section — one paragraph, no build.

## Deliver

Anyone can seed a second capture run without reverse-engineering a validator,
and there is exactly one policy shape in the system.

## Non-goals

- No new governed object type (deferred per R-C2 v2). No crawler/mapper/emit
  behavior change beyond the field-shape alignment. No CMS-Agent code changes.

## Acceptance criteria

- Engine tests green with the `ProjectCapturePolicy` shape; the 2026-08-13
  Zilberman policy passes through unchanged (test-proven).
- Template + runbook committed; a dry seed-to-score walkthrough in the runbook
  matches real command output.
- Committed `T12.7: …`; no PR.
