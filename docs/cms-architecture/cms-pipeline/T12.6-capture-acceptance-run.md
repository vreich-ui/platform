# T12.6 — HUMAN_GATE: capture acceptance run (Wolf reviews a real clone)

**Phase 12 · depends_on: T12.5 merged + deployed (the T11.11 staging-site precondition is met — the fleet is live with three tenants; `fernwell` is the standing synthetic tenant, or mint a fresh landing-zone tenant via the genesis line) · mode: human_gate · model: Sonnet 5 · effort: medium**
**Blocks:** W12 exit.

## Read first

`11-platformization-plan.md` §3 + §8 (W12 exit criteria), the OQ-W12-1/-2/-3 rulings, the T12.4/T12.5 run + report formats, `conversion-playbook.md` (run discipline; records rule).

## Context

The wave's proof: Wolf names a target; the pipeline — honoring that target project's contract-declared bounds (OQ-W12-1), with the model's hard refusals as the floor — produces a reviewable draft clone inside the landing-zone tenant (an existing tenant Wolf names, or a genesis-minted staging site); Wolf walks the result and the reports and signs off — or files the gaps.

## Scope — strictly sequential

1. Wolf names the target; the run reads the target project's contract-declared bounds (scope, rate, copy-rights posture) from its MCP contract (`registry_get` / `object_contract`) and records them verbatim in the run report. There is no global ownership gate — the target project's contract is the authority (OQ-W12-1), and the model's hard refusals are the floor beneath it.
2. Full pipeline: capture → map → theme → emit (landing-zone tenant) → fidelity loop; zero manual store edits.
3. Wolf reviews: the draft pages in the staging preview, the fidelity report against the OQ-W12-2 bar, the gap report, and the copy-policy log.
4. Disposition per Wolf: accept (optionally publish/release selected pages on the landing-zone tenant through the normal gates — publish commits with `[skip netlify]`; going live still requires the explicit `release_to_production`), or record the specific misses as W10-backlog entries / a composite-evidence update.
5. **Records (same change):** state-of-play entry with scores + refs; `11-platformization-plan.md` status flip; gap feed committed.

## Deliver

A signed-off, end-to-end demonstrated "point an agent at a site" capability with its limits documented — or a precise, evidence-backed list of what to grow next.

## Non-goals

- No exceeding the target project's contract-declared bounds, and the model's hard refusals always apply. No auto-publishing. No rubric-bending to pass.

## Acceptance criteria

- Run recorded with the target project's contract-declared bounds (quoted) honored, scores, and Wolf's disposition; docs updated in the same change.
- Committed `T12.6: …`; no PR.
