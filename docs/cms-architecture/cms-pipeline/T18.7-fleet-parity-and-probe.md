# T18.7 — Fleet parity: probe checks, create-site defaults, FLEET-STATUS, cross-site person seam

**Phase 18 · depends_on: T18.1, T18.4 · mode: auto · model: claude-sonnet-5 · effort: medium**
**Blocks:** T18.8.
**Plan:** `docs/cms-architecture/18-membership-plan.md` F11, §2.2 (person_id convergence), laws P1/P2 (`16-genesis-parity-plan.md`).

## Read first
- `docs/cms-architecture/16-genesis-parity-plan.md` P1–P5, `15-fleet-admin-parity.md`, `FLEET-STATUS.md`, `scripts/fleet-capability-probe.mjs`, `packages/core/cli/create-site.mjs` + `.test.ts`, `packages/core/cli/migrate-site.mjs`, `docs/cms-architecture/13-separation-plan.md` (the designed-not-built per-client separation — the person seam must not contradict it).

## Scope
1. **Probe**: add a `membership` family to `fleet-capability-probe.mjs`: users store reachable; policy present or default; sweep function declared in that site's `netlify.toml`; templates present; `/admin/accept` reachable (HTTP 200 HEAD on the deployed site when a URL is known). Non-secret only.
2. **create-site**: scaffold emits `config/membership-policy.ts` override stub (committed default is core's), the sweep shim + schedule block, the four templates (if T18.0c chose per-site copies), the checklist rows; `migrate-site.mjs` back-fills the same into an existing site; run it against all four tenants and commit the results (P1).
3. **FLEET-STATUS.md**: rows per tenant: Identity enabled ☐, invite-only ☐, templates set ☐, first stored Owner ✓/☐, `ADMIN_EMAILS` still relied on ✓/☐, sweep declared ✓ — the ☐ ones are Wolf's console clicks (T18.9).
4. **Cross-site person seam** (design + minimal code): document in plan §2.2 how the deterministic `person_id` lets a future `fleet-admin` list "Wolf: owner on drlurie/platform/fernwell/zilberman" by reading each site's store; add `packages/core/server/lib/membership/fleet.ts` with `listMembershipsForPerson(stores[], person_id)` (pure, tested, no caller yet) so the seam exists.
5. **`ENV_CHECKLIST` / T11.7 env table**: no new env vars were introduced by W18 (assert this in the commit body; if any task did add one, land it in the table + every site's env per P2).

## Deliver
Every tenant is provably at the same membership footing, the probe says so, and the fleet-admin seam exists.

## Acceptance
- `npm test`, `npm run check` green; probe run output pasted into state-of-play for the four sites (redact nothing — it must be non-secret by construction).
- Commit `T18.7:`.
