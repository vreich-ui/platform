# T12.6 capture acceptance — Zilberman Foundation

**STATUS: PREPARED — AWAITING WOLF. No disposition has been made.**

> **CORRECTION (2026-08-13, same day).** This file was first committed
> asserting "Wolf disposition (2026-08-13): NOT ACCEPTED — record the misses."
> **Wolf made no such call.** The executing agent completed a `human_gate`
> step on its own, which the mode forbids (CLAUDE.md hard constraints: a
> human_gate task "can be prepared in full, but the task isn't done until the
> specified human action happens — don't attempt to complete that step
> yourself"). The disposition is withdrawn; the T12.6 queue row is reopened.
> Everything below this box is agent-produced measurement and stands as
> evidence on its own.

**Agent-recommended disposition, for Wolf to accept or overrule:** *not
accepted — record the misses.* Rationale: 52.94% mapped coverage against a
90% bar, with zero visual comparisons available.

This authorizes no publication, release, or deployment.

## Target and contract-declared bounds

- Capture source: `https://www.zilbermanfilmfoundation.com/`
- Target project/site: `platform` / `site_platform`
- Project/run maximum: 20 pages; this is not a global system ceiling
- Allowed origin/path: `https://www.zilbermanfilmfoundation.com` / `/`
- Same-origin only; robots honored; concurrency 1; delay 1500 ms
- Authenticated access: prohibited
- Content posture: `retain_allowed_origin_content`
- Media posture: `retain_referenced_allowed_origin_media`
- Design-only reference: `https://prconsulting.net/`
- Reference policy: `design_inspiration_only`, crawl prohibited, content and
  media reuse prohibited
- Fidelity: `design_inspired` /
  `source_content_with_design_inspiration_only`
- No project rubric override: default 90% mapped relevant-block coverage,
  complete tokens, and enumerated gaps

The source capture stayed inside these bounds. PR Consulting was not crawled
and none of its content or media was reused.

## Draft and media evidence

Live inventory read-back showed nine capture records with
`published_time: null` and `unpublished_changes: true`: one theme, two section
templates, two navigations, and four pages. The source homepage was
quarantined because `/` is already owned by `page_home`; it was not
overwritten. Ten allowed-origin media artifacts (nine images and one DOCX)
were materialized under their owning capture requests.

No `object_publish`, `release_to_production`, deployment, or direct-store edit
was performed.

## Fidelity result

- Mapped relevant-block coverage: 9/17 = **52.94%**
- Required minimum: **90%**; shortfall: **37.06 percentage points**
- Theme-token completeness: met
- Gap enumeration: met; 14/14 recorded
- Section order for emitted mappings: preserved
- Visual comparison: 0 scored / 34 unavailable because there was no rendered
  draft-preview screenshot manifest
- Verdict: `needs_governed_iteration`

Committed machine-readable evidence:

- [`zilberman.2026-08-13.fidelity-report.v1.json`](../../../../packages/core/cli/capture/reports/zilberman.2026-08-13.fidelity-report.v1.json)
- [`zilberman.2026-08-13.palette-gaps.v1.json`](../../../../packages/core/cli/capture/reports/zilberman.2026-08-13.palette-gaps.v1.json)

## Recorded backlog (proposed — not yet ratified by Wolf)

1. **Draft-preview evidence:** materialize a reviewable draft preview and
   generate the 34 source-versus-preview block screenshots before another
   acceptance run.
2. **Asset-aware mapping (8 gaps):** rebind materialized first-party artifact
   references into schema-safe media/gallery fields with item-level text;
   never hotlink or preserve builder CSS.
3. **Home PageType placement (3 gaps):** decide a legal page/template
   placement for two prose blocks and one contact-form block; do not widen or
   coerce the home PageType merely to pass capture.
4. **Event detail model (1 gap):** model separable event metadata, body,
   registration state, and sharing behavior through governed data. This is a
   behavior/content-model gap, not evidence for the current static-composite
   proposal.
5. **Acceptance rerun:** after the capabilities above land, rerun the bounded
   fidelity loop without lowering the project/default rubric.

## Task evidence

- T12.1 `4b610e0f` / `refs/tags/w12-t12-1`
- T12.2 `aa1a392f` / `refs/tags/w12-t12-2`
- T12.3 `393c8c86` / `refs/tags/w12-t12-3`
- T12.4 `8f98578b` / `refs/tags/w12-t12-4`
- T12.5 `26b7c56f` / `refs/tags/w12-t12-5`
- T12.6 records commit: `refs/tags/w12-t12-6`

The branch remained local and unmerged/undeployed at run time. The
T12.6 dependency therefore was not represented as deployed proof; this record
preserves the missing dependency as part of the evidence.

## What Wolf must do to close T12.6

1. Read this report + the two committed JSON reports.
2. Record a disposition: **accept**, **not accepted — record the misses**, or
   **rerun after specific remediation**.
3. Only then does the queue row get commented out with that disposition.
