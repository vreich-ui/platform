# T18.0c — Identity e-mail templates, provisioning checklist, parity audit + runbook rewrite

**Phase 18 · depends_on: T18.0b · mode: auto · model: claude-sonnet-5 · effort: low**
**Blocks:** T18.9 (Wolf's credentialed run needs the per-site console steps written down).
**Plan:** `docs/cms-architecture/18-membership-plan.md` — §4.1 step 2, F10, F11 (V1 part), law P1/P2.

## Why this model / effort
Templates, checklist rows, docs, and one audit check. Deterministic. Sonnet, low.

## Read first
- Plan §4.1 step 2 and §8 row T18.0.
- Netlify "Identity-generated emails" facts (verified): custom template files are committed in the site's publish directory and their **path** is set per site in *Project configuration → Identity → Emails* (Invitation / Confirmation / Recovery / Email change). Template variables: `{{ .SiteURL }}`, `{{ .Token }}`, `{{ .Email }}`, `{{ .NewEmail }}`. Custom link form: `{{ .SiteURL }}/admin/accept/#invite_token={{ .Token }}` (etc.).
- `packages/core/cli/create-site.mjs` (~L117-200: the env/console checklist rows; the `ADMIN_EMAILS` row and the "Enabling Identity is the real gate" note).
- `scripts/audit-site-admin-parity.mjs` and `scripts/fleet-capability-probe.mjs` (~L97-106: the access/identity env family notes).
- `docs/cms-architecture/site-provisioning-runbook.md` §"Identity / roles" (~L85 and L160-190 — step 3 "Invite the first Owner" is the one that was not executable).
- `docs/cms-architecture/16-genesis-parity-plan.md` P1/P2 (any new file every site must carry lands on all four sites in the same change).

## Scope
1. **Templates (core-owned, copied to every site's public dir at build):** `packages/core/app/emails/identity/{invitation,confirmation,recovery,email-change}.html` — plain, brand-neutral HTML using the site's name via `{{ .SiteURL }}` only (Identity templates cannot read our config); every link points at `/admin/accept/#<token_key>={{ .Token }}`. Wire them into the app so each site publishes them at `/emails/identity/*.html` (check how `packages/core/app` already contributes static assets to a site build — follow that mechanism; if none exists, add the four files to each `sites/<client>/public/emails/identity/` **and** to the create-site scaffold, P1).
2. **`create-site.mjs` checklist:** add four console rows under the Identity gate: "Emails → Invitation template path = `/emails/identity/invitation.html`" (and the other three), plus "Registration = Invite only" and "External providers = Google (optional)". Rows print with the ☐ convention the checklist already uses. Extend `create-site.test.ts` for the new rows.
3. **`scripts/audit-site-admin-parity.mjs`:** add a check that `sites/<client>` serves the four template files (file presence in the built/public tree) and that `/admin/accept` is a known admin route. Update `tests/scripts/admin-parity.test.mjs`.
4. **`scripts/fleet-capability-probe.mjs`:** add a non-gating "identity" family note listing the console-only prerequisites (Identity enabled, invite-only, template paths) so the probe output tells a human what to click; do not attempt to call the Netlify Identity admin API.
5. **Runbook rewrite** (`site-provisioning-runbook.md` Identity section): step 1 Identity enable + invite-only + template paths (with exact console navigation), step 2 `ADMIN_EMAILS`, step 3 "sign in, invite yourself as Owner from `/admin/settings/admins`, accept from the e-mail on `/admin/accept`, then (T18.1) promote and remove the env row". Add a "Recovering a stuck invite" box: re-send from Netlify UI Identity tab → link now lands on `/admin/accept`.
6. `docs/KNOWN_ISSUES.md`: add entries for F1/F2/F3 marked FIXED-BY T18.0a/b/c with the one-line symptom each ("invite link lands on the offer page"…), so support searches find them.
7. `docs/cms-architecture/FLEET-STATUS.md`: add a column/row block "Identity e-mail templates set (console)" ☐ for each of the four tenants — Wolf ticks these during T18.9.

## Deliver
Every tenant ships the four templates; the console steps a human must click are printed by `create-site`, checked by the parity audit, and written in the runbook.

## Non-goals
No Netlify console changes (human-only, T18.9). No code in `goTrueClient`/`admin-users`.

## Acceptance criteria
- `npm test`, `npm run check` green; `node scripts/audit-site-admin-parity.mjs --site sites/<each of four>` passes with the new checks.
- All four `sites/<client>` carry the templates (P1) — `git status` shows the same four files added per site (or the core mechanism proves they are emitted at build; state which in the commit body).
- Commit `T18.0c:`; state-of-play entry.
