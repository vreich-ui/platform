# T16.10 — Genesis entry points + the new-client acceptance doc

**Phase 16 · depends_on: T16.8 (verify exists to be documented) · mode: auto · model: Sonnet 5 · effort: low**
**Blocks:** —.

## Read first

Plan §1.2 item 10; `site-provisioning-runbook.md` (human half),
`T11.7-provisioning-cli.md` (env table), `T14.3-checklist.md` (worked
instance), the platform manual genesis page content
(`sites/platform/data/site/pages/page_manual_genesis.json`), the fernwell
timings in state-of-play (the T14.9 baseline table).

## Context

Genesis is discoverable today only by archaeology: no npm entry point, the
knowledge split across five documents. Client #4 should be born from one
command sequence and one checklist.

## Scope

- Root `package.json` scripts: `site:create` → `create-site.mjs`,
  `site:genesis` → `site-genesis-drive.mjs`, `site:verify` →
  `site-genesis-drive.mjs --verify`, `fleet:parity` →
  `audit-site-admin-parity.mjs --all`, `fleet:capability` →
  `fleet-capability-probe.mjs --all` (args pass through; no behavior change).
- New `docs/cms-architecture/new-client-acceptance.md`: the ONE ordered
  checklist spanning stage 0 → stage 1 → verification — each step names the
  command, its proof (which audit/test/probe goes green), and whether it is
  agent or account-authority work. LINK to the runbook/T11.7 for detail
  rather than duplicating tables (one source of truth each; this doc is the
  spine, not a copy). End with the stage-2 onboarding handoff list (voice,
  tracking, branding, products) marked explicitly out of genesis scope.
- Cross-link: runbook header + plan doc §2 + README fleet section point at
  the acceptance doc.

## Acceptance criteria

- A reader can birth a client from `new-client-acceptance.md` alone without
  opening any other doc except at the linked detail points; every command
  in it exists as an npm script and runs `--help`/`--dry-run` clean.
- Committed `T16.10: …`; no PR.

## Non-goals

No behavior changes to any script; no runbook rewrite; no doc deletion.
