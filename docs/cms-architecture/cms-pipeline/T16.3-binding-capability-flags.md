# T16.3 — Binding capability flags fleet-wide

**Phase 16 · depends_on: T16.0 (soft) · mode: auto · model: Sonnet 5 · effort: low**
**Blocks:** —.

## Read first

Plan §1.2 item 5; `packages/core/server/lib/site-binding.ts`;
`sites/drlurie/config/site-binding.ts` (the reference — note the
`warmAdminKeepalive` comment: ~1.3s admin TTFB without it);
`sites/{platform,fernwell}/config/site-binding.ts`; `create-site.mjs`
site-binding template.

## Scope

- Add to platform + fernwell bindings: `warmAdminKeepalive: true`,
  `adminLabel`, committer name/email (parameterized per site, matching the
  drlurie pattern, no invented branding — slug-derived defaults are fine).
- create-site template emits the same by default.
- Unify the export name: drlurie exports `drlurieSiteBinding` while
  CLI-born sites export `siteBinding` — give drlurie a `siteBinding` alias
  (keep the old name exported for any importer; verify importers first per
  the known-gotchas rule).
- `admin-parity.mjs` check (`binding-capability`): every site binding
  declares the capability flag set the create-site template emits.

## Acceptance criteria

- Parity check green 3/3; admin cold-start keepalive active on all tenants
  next deploy. Committed `T16.3: …`; no PR.

## Non-goals

No env-name changes (`PLATFORM_ENV_NAMES` stays universal — OQ-W11-4). No
`assetHost`/`imageDomains` changes (client data, unshipped).
