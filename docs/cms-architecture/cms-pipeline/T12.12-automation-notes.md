# T12.12 automation notes — measured genesis run, 2026-08-14

> The brief's secondary purpose: every manual step of minting `sites/zilberman`,
> executed and measured, so T12.11's genesis driver knows exactly what the
> Netlify API can absorb. This run was executed by the orchestrator session
> under Wolf's live delegation (his Netlify CLI token, gcloud auth, and git
> credential, operated from his machine) — so "human" below means *requires
> account authority*, not *requires hands on a console*: almost nothing did.

## Measured step table

| # | Step | How it ran | API-automatable? | Notes for the T12.11 genesis driver |
|---|------|-----------|------------------|-------------------------------------|
| 1 | Scaffold `sites/zilberman` (80 files) | `create-site.mjs` | ✅ already automated | Byte-identical across independent regenerations. |
| 2 | Root `package-lock.json` workspace entry | `npm install` + commit | ✅ (mechanical) | **Hit for real:** first build failed `npm ci` EUSAGE ("Missing: @fleet/site-zilberman from lock file"). The driver must regenerate + commit the lock WITH the scaffold, or the first build always fails. |
| 3 | Netlify site create (`zilbermanfilmfoundation`) | `create-site --netlify-token` | ✅ | Idempotent lookup-by-name confirmed. |
| 4 | Per-site secrets (PUBLISH_SECRET, MCP_HTTP_AUTH_TOKEN, ARTIFACT_UPLOAD_TOKEN_SECRET, TRACKING_SALT, PURCHASE_TOKEN_SECRET) | provisioning auto-mint | ✅ already automated | Values never surfaced. |
| 5 | pdf-tool bridge (PDF_TOOL_BASE_URL / AGENT_RUN_TOKEN) | provisioning auto-inherit | ✅ already automated | |
| 6 | Fleet-shared **readable** env (ADMIN_EMAILS, ROLE_EMAILS_*, GITHUB_BRANCH/REPOSITORY/AUTHOR_*, CMS_AGENT_MCP_ENDPOINT, CMS_AGENT_CHAT_MODE, SECRETS_SCAN_OMIT_KEYS) | Netlify env API copy from a sibling | ✅ | |
| 7 | Fleet-shared **secret** env (GITHUB_CONTENT_TOKEN, ANTHROPIC_API_KEY, OPENAI_API_KEY, NETLIFY_AUTH_TOKEN, NETLIFY_BLOBS_TOKEN) | secret custodian, NOT sibling copy | ⚠️ needs a custodian | **Trap, hit for real:** the env API returns MASKED values (`****…`, 20 chars) for `is_secret` vars — a "copy from sibling" writes garbage that 401s at runtime. Real values must come from a custodian: this run used GCP Secret Manager (anthropic-api-key, openai-api-key), the operator git credential (GITHUB_CONTENT_TOKEN), and the operator account token (NETLIFY_AUTH_TOKEN / NETLIFY_BLOBS_TOKEN — **flagged for rotation to dedicated per-site PATs**, sibling sites each carry distinct 40-char PATs). Netlify secrets also refuse `all` context and the `post_processing` scope — write per-context values with scopes builds/functions/runtime. |
| 8 | Repo/build binding (repo, branch, base=sites/zilberman, cmd, dir) | `PATCH /sites/{id}` with the GitHub App `installation_id` read from a sibling site | ✅ | |
| 9 | Build hook + `NETLIFY_BUILD_HOOK_URL` | `POST /sites/{id}/build_hooks` + env API | ✅ | Provisioning does NOT do this today; `release_to_production` refuses without it (`build_hook_not_configured`, hit for real). Driver should absorb both halves. |
| 10 | **Netlify Identity enable** | `POST /api/v1/sites/{id}/identity` → 201 | ✅ | **The runbook's "console-only" note is stale.** Only the "Invite only" registration preference wasn't reachable via API (404) — that one click remains for Wolf. |
| 11 | `IDENTITY_URL` env | env API | ✅ | |
| 12 | Scoped CMS-Agent bearer (`mcp-scoped-tokens-json` entry + `CMS_AGENT_MCP_TOKEN` on the site) | gcloud Secret Manager + env API | ✅ (with GCP authority) | Minted `{projects:[zilberman], toolAllowlist:[agent_resolve, agent_converse]}` — deliberately narrower than siblings' 6-tool scope; widen by explicit decision only. |
| 13 | CMS-Agent project registration + capture policy | `project.create` + `project.update` (capturePolicy copied verbatim from the ratified policy on `platform`) | ✅ already MCP | `project.test_connection` green same run. |
| 14 | Cloud Run connection env (`ZILBERMAN_MCP_ENDPOINT` + `ZILBERMAN_MCP_TOKEN`=`zilberman-mcp-token:latest`) | gcloud `--update-env-vars`/`--update-secrets` | ✅ (with GCP authority) | **Follow-up:** `cloudbuild.deploy.yaml`'s env/secret lists in CMS-Agent don't carry the ZILBERMAN pair yet — a future cloudbuild deploy keeps them (update-* semantics) but the manifest should name them. |
| 15 | Seed drive (15 objects create→publish→release) | `site-genesis-drive.mjs` | ✅ already automated | **Trap, hit for real:** env changes reach functions only after a REBUILD — drive after the env-bearing deploy, not before. One transient GitHub-API 500 on an export commit; the drive's idempotent re-run converged. |
| 16 | pdf-tool storage grant (PDF_TOOL_STORAGE_SITE_ID/TOKEN) | — | ❌ human | Dedicated Netlify machine account + Blobs-scoped PAT; no API mints accounts. Outstanding. |
| 17 | First-Owner sign-in + invite | — | ❌ human | Wolf at `/admin` with an ADMIN_EMAILS address. Outstanding. |
| 18 | DNS | skipped | — | R-C4: netlify.app serving only. |

## Net for T12.11

Everything except rows 16–17 (and the one Identity-preference click in row 10)
is API-automatable **given a secret custodian and account tokens** — the
driver's humanChecklist can shrink to: storage-grant PAT, first-Owner
sign-in, Identity registration preference, and custodian-missing secrets.
