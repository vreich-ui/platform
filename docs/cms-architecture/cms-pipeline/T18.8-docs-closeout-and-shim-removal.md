# T18.8 — Docs closeout, deprecated shim removal, CLAUDE.md pointer

**Phase 18 · depends_on: T18.3b, T18.5, T18.6b, T18.7 · mode: auto · model: claude-sonnet-5 · effort: low**
**Blocks:** T18.9.

## Read first
- Everything under `docs/cms-architecture/18-membership-plan.md`; the state-of-play entries T18.0a…T18.7 wrote; `CLAUDE.md` (the W-sections at the top — add a short W18 paragraph in the same voice: what governs, the two things an agent will hit in ordinary work); `docs/cms-architecture/10-admin-workspace-plan.md` §6/§8 (mark the users-store/roles rows as superseded by 18-membership-plan.md §2/§6; resolve OQ-W9-4 "visible third tier" as DONE); `docs/KNOWN_ISSUES.md`.

## Scope
1. Remove the `users-store.ts` v1 adapter shim if no importer remains (grep proof in the commit body), delete `user-invite.ts` if T18.2 left a shim, delete `app/utils/netlifyIdentityLoader.ts` (unused since before W18 — cite the grep).
2. Rewrite plan §8 (task table) into a "what shipped" table with commit refs (bare short SHAs, never full URLs — CLAUDE.md gotcha) and flip §1 findings to ✅/⏳ with the closing task.
3. `CLAUDE.md`: W18 paragraph (≤ 12 lines) + the two rules: "membership writes require a human principal — agents get `membership_requires_human`", "any e-mail from Identity lands on `/admin/accept`; never handle tokens elsewhere".
4. `10-admin-workspace-plan.md` §8 → pointer + OQ-W9-4 resolved; `site-provisioning-runbook.md` re-read for consistency with T18.0c's rewrite; `KNOWN_ISSUES.md` entries closed.
5. `docs/cms-architecture/decisions/2026-08-17-membership-defaults.md`: record the four §9 defaults as rulings-by-default under R8, with the queue-comment override mechanism, and space for Wolf's signature line.

## Acceptance
- `npm run check`, `npm test` green (shim removal compiles). Commit `T18.8:`.
