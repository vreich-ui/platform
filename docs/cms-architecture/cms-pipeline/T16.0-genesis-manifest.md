# T16.0 — Genesis manifest: one staged source of truth

**Phase 16 · depends_on: — · mode: auto · model: Opus · effort: medium**
**Blocks:** T16.1, T16.2, T16.3, T16.8.

## Read first

`docs/cms-architecture/16-genesis-parity-plan.md` §2 (the four-stage line —
this task implements it), `packages/core/cli/create-site.mjs` (`buildPlan()`,
`DATA_SITE_SUBDIRS`), `scripts/site-genesis-drive.mjs` (`SEED_MODULES`),
`packages/core/cli/admin-parity.mjs` (checks 13–14, the point fixes this
generalizes).

## Context

The genesis seam is enforced in three unsynchronized lists; both 2026-08-05
addenda were the same failure (added to one list, not the others). One
committed manifest ends the class.

## Scope

- New `packages/core/cli/genesis-manifest.mjs` exporting a single structure:
  every seed module name, every `data/site/` subdir, and every scaffold file
  group, each tagged with its stage (`scaffold` | `genesis` | `onboarding`)
  per plan §2. Pure data + tiny accessors; no side effects.
- `buildPlan()`, `DATA_SITE_SUBDIRS`, and `SEED_MODULES` become consumers —
  derive their lists from the manifest (genesis-stage entries only for the
  drive; onboarding-stage seed files are scaffolded but not driven).
- Drift test (`tests/scripts/genesis-manifest.test.mjs`): for each of the
  three sites, every manifest entry exists where its stage says it must
  (seed file present, subdir present), and no consumer carries an entry the
  manifest doesn't. Must fail today for the known gaps (voice, templates on
  platform) — mark those with `todo:'T16.1'` expected-failure annotations so
  the suite is green but the gaps are machine-recorded, and T16.1 deletes
  the annotations.
- Wire the test into the existing fleet CI job.
- CLAUDE.md: insert the following section verbatim, immediately after the R8
  blockquote and before "## Definition of \"converted\"" (this was authored
  with the plan on 2026-08-10 but deliberately left for this task to land via
  local git rather than a whole-file API push):

  > ## W16 — GENESIS SCOPE + FLEET PARITY LAW (Wolf, 2026-08-10, GOVERNING)
  >
  > Genesis and cross-tenant parity are governed by
  > [`docs/cms-architecture/16-genesis-parity-plan.md`](docs/cms-architecture/16-genesis-parity-plan.md):
  > the four-stage line (scaffold → genesis → onboarding → content; genesis =
  > the first two only, per the 2026-08-05 types-not-instances ruling) and laws
  > P1–P5. The two you will hit in ordinary work: **P1 — any change under
  > `packages/core` (incl. the create-site templates) that alters what a site's
  > repo tree, netlify.toml, env set, or seed pack must contain is INCOMPLETE
  > until applied to every existing `sites/<client>` in the same change.**
  > **P2 — any new env var read by core lands, in the same change, in the T11.7
  > env table + `ENV_CHECKLIST` + every existing site's env (or degrades with
  > an explicit catalogued `error_code`) and is covered by the capability
  > probe.** Tool surfaces are uniform across tenants except via the documented
  > `OPTIONAL_HANDLER_TOOLS` mechanism with a recorded ruling (today:
  > `verify_article_images` only). Execution: the W16 rows in queue.tsv.

  (Strip the leading `> ` quoting when inserting — it is a fenced quotation
  here only so this brief stays unambiguous.)

## Acceptance criteria

- One place to add a genesis item; forgetting any consumer turns CI red.
- create-site dry-run fixture regenerated only if output actually changed
  (it should not — this task rearranges sources, not output).
- Committed `T16.0: …`; no PR.

## Non-goals

No new seed content, no netlify.toml keys (T16.2), no store access.
