# T16.1 — Seed-pack completion: voice + templates join the manifest (repo half)

**Phase 16 · depends_on: T16.0 · mode: auto · model: Sonnet 5 · effort: medium**
**Blocks:** T16.9 (its store half).

## Read first

Plan §1.2 items 2–3 and §2 stage definitions; Wolf's 2026-08-05 ruling in
`docs/cms-architecture/15-tenant-retrofit-checklist.md` (types yes, invented
instances no); `sites/drlurie/seeds/voice-seed-data.mjs` and
`sites/fernwell/seeds/templates-seed-data.mjs` (the shapes to parameterize).

## Context

Voice is fleet-real (all three sites have the seed file) but belongs to no
stage; templates are genesis-stage but platform lacks the seed entirely.
Respect the ruling: voice/tracking are ONBOARDING stage — scaffolded as
skeletons, never driven by genesis with invented content.

## Scope

- Manifest: add `voice` and `tracking-config` as onboarding-stage seed
  modules + `data/site/voice/` as an onboarding subdir; templates confirmed
  genesis-stage. Delete the T16.0 expected-failure annotations this closes.
- `create-site.mjs`: emit `voice-seed-data.mjs` and
  `tracking-config-seed-data.mjs` SKELETONS for a new client (structure and
  ids parameterized, content placeholders clearly marked `onboarding: fill
  with the client`), and emit `templates-seed-data.mjs` (already done —
  verify against the manifest).
- Backfill `sites/platform/seeds/templates-seed-data.mjs` (parameterized
  copy of fernwell's, platform ids) and `mkdir` + `.gitkeep`
  `sites/fernwell/data/site/voice/`.
- Backfill `sites/platform/seeds/tracking-config-seed-data.mjs` skeleton
  matching its committed `data/site/tracking.json` (see plan §1.2 item 8 —
  the seed must describe what is live, not invent).
- Regenerate the create-site dry-run fixture (file count will grow).

## Acceptance criteria

- Manifest drift test green with zero expected-failure annotations.
- Dry-run fixture reflects the new files; `admin-parity` unchanged or better.
- No store calls anywhere. Committed `T16.1: …`; no PR.

## Non-goals

No live objects (T16.9 drives fernwell voice / platform templates into the
stores). No voice content invention for any tenant.
