# T16.7 — @drlurie/core → @fleet/core

**Phase 16 · depends_on: T16.6 (fixed lint proves completeness) · mode: auto · model: Sonnet 5 · effort: medium**
**Blocks:** —.

## Read first

Plan §1.2 item 9; `packages/core/package.json`; `create-site.mjs` (~lines
662–664, the stamp); each `sites/*/package.json`; root `package.json` +
`package-lock.json` (workspace links).

## Context

Core is fleet law with a client's name on it, and the scaffolder stamps that
name into every new tenant. Audit verified NO code imports the specifier —
occurrences are package.json dependency links, descriptions, and prose.

## Scope

- Rename the package to `@fleet/core`; update every dependent package.json
  (drlurie, platform, fernwell, root workspaces if referenced), the
  create-site stamp (both the dependency key and the description string),
  and regenerate `package-lock.json` (`npm install` at root; commit the
  lockfile delta — expect it to be link-shaped only).
- Grep-verify with the FIXED lint (T16.6): the temporary allowlist entry
  for `@drlurie/core` is deleted in this commit; the lint must pass with
  zero exemptions.
- Regenerate the create-site dry-run fixture (the stamp appears in it).
- Docs: do NOT rewrite history in state-of-play/old briefs; update only
  live references (README, runbook, plan docs) if they name the package.

## Acceptance criteria

- `npm run build` + test suite green from a clean install; no
  `@drlurie/core` outside historical logs; lint exemption list empty.
- Committed `T16.7: …`; no PR.

## Non-goals

No repo rename, no directory moves, no import-path changes (none exist).
