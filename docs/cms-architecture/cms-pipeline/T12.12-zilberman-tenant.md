# T12.12 — HUMAN_GATE: mint `sites/zilberman` as fleet tenant #4

**Phase 12 · depends_on: T12.7 · mode: human_gate · model: Sonnet 5 · effort: medium**
**Blocks:** the T12.6 acceptance rerun; informs T12.11's genesis automation.

## Read first

`decisions/2026-08-13-capture-productization-rulings.md` (**R-C4 governs**),
`docs/cms-architecture/site-provisioning-runbook.md`,
`packages/core/cli/create-site.mjs` `--dry-run` output,
`docs/cms-architecture/16-genesis-parity-plan.md` (scaffold → genesis →
onboarding → content), `T14.9-synthetic-third-site.md` (fernwell — the
precedent), `docs/cms-architecture/conversion-playbook.md`.

## Context

R-C4: the acceptance rerun lands in a real tenant for a real property. The
existing `vreich-ui/zilbermanfilmfoundation` repo is a plain static site with
no core and no `/mcp` — a landing zone must run the engine. Fourth tenant, same
core. **Secondary purpose:** this run is executed with notes — every manual
step, its duration, and whether the Netlify API could have done it — because
T12.11's genesis driver automates exactly this list.

## Scope — strictly sequential

1. **ME (agent):** `create-site --name zilberman --dry-run`; review tree, env
   table, seed pack. Fix anything genesis-stage that would block a clean mint.
2. **WOLF:** account-authority steps — Netlify site (serving at
   `zilbermanfilmfoundation.netlify.app`), env set incl. `MCP_HTTP_AUTH_TOKEN`,
   runbook secrets steps.
3. **ME (agent):** store probe green; baseline seed pack driven in; the site
   renders its seeded pages at its own URL.
4. **ME (agent):** round-trip proof — one page created via MCP → patched →
   published → released; `object_inventory` returns it; the other three
   tenants show ZERO change.
5. **ME (agent):** fleet parity — `audit-site-admin-parity --all` passes at
   full check count ×4; capability probe green. Register the tenant in
   CMS-Agent (`project.create` + registration contract) with a real
   `capturePolicy` for the Zilberman source.
6. **Records (same change):** state-of-play entry, FLEET-STATUS row, CLAUDE.md
   tenant list (three → four), the automation-notes file for T12.11.

## Deliver

A fourth isolated tenant ready to receive a capture, plus the measured list of
which genesis steps the Netlify API can absorb.

## Non-goals

- No capture run here — minting only. No public DNS beyond netlify.app. No
  content beyond the baseline pack + proof page.

## Acceptance criteria

- All proofs recorded with commit/deploy refs; four tenants green in fleet CI
  + parity audit; other three tenants provably unchanged.
- CMS-Agent registration verified via `project.test_connection`.
- Automation-notes committed (feeds T12.11).
- Committed `T12.12: …`; no PR.
