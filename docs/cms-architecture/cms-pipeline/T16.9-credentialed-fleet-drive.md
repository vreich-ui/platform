# T16.9 — Credentialed fleet drive: store backfill + live capability matrix

**Phase 16 · depends_on: T16.1, T16.5, T16.8 · mode: notify · model: Sonnet 5 · effort: medium**
**Blocks:** —.

## Read first

Plan §1.2 items 2, 3, 8 and law P3; `15-tenant-retrofit-checklist.md`
(the ruling on what a tenant may/may not carry);
`docs/cms-architecture/FLEET-STATUS.md` (fernwell's admin-nav-link row is
still ❌ — this drive closes it); the conversion playbook (lifecycle recipe).

## Context

Run interactively with the session MCP connections (drlurie, platform,
fernwell endpoints). This is the store half of W16: everything repo-side is
already merged; this drive makes the three stores match the manifest.

## Scope — in order, per the genesis order law where creating

1. `site-genesis-drive.mjs --verify` on all three sites; record the matrix.
2. Backfill per verify output, via each site's own `/mcp`:
   - fernwell: instantiate the three starter templates from its seed;
     fix the admin nav link (the documented gap — publish + release);
     voice stays ABSENT (onboarding stage — record as INFO, not a gap).
   - platform: create the three starter templates from T16.1's seed
     (keep `tpl_object_reference` untouched); reconcile the tracking
     inversion — its committed `tracking.json` must equal what a
     `tracking_config` object in ITS store says, so create/publish that
     object from T16.1's skeleton seed matching live values (no invented
     analytics providers — copy what is live, nothing more).
   - drlurie: verify only; it seeds tracking but exports none — if verify
     shows its store `tracking_config` is real and live behavior depends on
     it, materialize the export; if the object is absent, record the seed as
     onboarding-stage-not-yet-run. Decide from store truth, record either way.
3. `fleet-capability-probe.mjs --all`; paste the matrix into FLEET-STATUS.
   Any red cell: fix env via API where the value is derivable (the pdf
   bridge pattern from create-site's provisioning code); anything needing
   account authority goes to Wolf as a named env var + site, not a question.
4. Re-run `--verify` on all three: clean. One release per site touched.
5. Record: FLEET-STATUS tenant table + capability matrix, state-of-play
   entry, object-inventory rows for every object created (same change).

## Acceptance criteria

- `--verify` exit 0 on all three sites; capability matrix committed with
  every cell green or explicitly ruled per-site; fernwell admin link live.
- All five conversion criteria hold for every object this drive created.
- Committed `T16.9: …`; no PR.

## Non-goals

No voice content authoring for any tenant. No new tool building — if the
probe exposes a missing tool/contract entry, that is a new task, flagged.
