# T16.6 — Fix the site-literal lint blind spot and the vacuous CSP gate

**Phase 16 · depends_on: — · mode: auto · model: Sonnet 5 · effort: low**
**Blocks:** T16.7 (the fixed lint must be in place before the rename lands, so the rename is provably complete).

## Read first

Plan §1.2 item 7; `tests/scripts/core-no-site-literals.test.mjs` (note its
own comment about the failure mode it then re-commits);
`tests/netlify/csp-drift.test.ts` (line ~75 reads a nonexistent
`sites/drlurie/data/site/tracking.json`; line ~62 reads only root toml).

## Scope

1. **Lint stripper**: the regex `/\/\*[\s\S]*?\*\//g` treats `/*` inside
   redirect-glob string literals (`/pdf/*`, `/admin/content/*`) as a comment
   opener — 40 % of `create-site.mjs` goes unscanned. Replace with a
   tokenizer-grade strip (smallest correct fix: strip string literals FIRST,
   then comments — or use a proper JS tokenizer if one is already in the
   dev deps; do not hand-roll a second clever regex). Also extend the walk
   to `package.json` files under `packages/core`.
   Then rerun: every NEW hit the fixed lint finds is either (a) the
   `@drlurie/core` stamp — add a single explicit allowlist entry
   `@drlurie/core (until T16.7)` with that comment, or (b) a real leak —
   fix it in this commit and say so in the commit message.
2. **CSP gate**: point it at per-site truth — for each site, read THAT
   site's committed tracking export if present (today: platform's
   `data/site/tracking.json`) and THAT site's netlify.toml (root for
   drlurie). The test must fail if a site has tracking providers enabled
   with no CSP-RO coverage for them. Depends on T16.2's headers existing on
   all tomls — if T16.2 hasn't landed yet, land this with the platform row
   marked expected-fail annotated `T16.2`.

## Acceptance criteria

- A fixture proving the old blind spot (glob string before a real literal)
  now fails the lint; full suite green otherwise.
- CSP gate demonstrably non-vacuous: flipping a provider on in a fixture
  without a header turns it red. Committed `T16.6: …`; no PR.

## Non-goals

No new CSP policy content; no rename itself (T16.7).
