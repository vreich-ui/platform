# T12.2 — Decomposition mapper: snapshot → section-palette mapping + gap report

**Phase 12 · depends_on: T12.1 · mode: notify · model: Fable · effort: xhigh**
**Blocks:** T12.4.

## Read first

`capture-spike-findings.md` + the fixture `snapshot.v1`, `packages/core/lib/registry/components/` (every type's data schema + `useWhen` — the mapping TARGET vocabulary; path updated 2026-08-13 for the W11/W14 core relocation), `docs/cms-architecture/design-vocabulary-gaps.md` (what W10 minted), `11-platformization-plan.md` §3.

## Context

The wave's hardest generative problem: turn a snapshot into (a) a per-page ordered list of `{sectionType, data, confidence}` candidates expressed ONLY in the registered vocabulary, and (b) a **palette-gap report** for every block nothing expresses. The mapper proposes; validation stays the law (its output must survive `object_validate` unchanged).

## Scope — files to create/modify

- ✚ `packages/core/cli/capture/map.mjs` (+ lib modules): heuristic + model-assisted mapping from `snapshot.v1` blocks to section instances — copy extracted into the type's real fields (headings/body/actions/cells), imagery recorded as asset-manifest refs (NOT hotlinks — the artifact pipeline handles media later), links as `route`/`external` targets per the NavTarget rules.
- Confidence per block; below-threshold blocks land in the gap report (`{blockRef, screenshotRef, why, nearestType, missingCapability}`) — the OQ-W8/W10 evidence feed.
- Nav mapping: discovered header/footer → `navigation.v1` candidate bodies (depth ≤ 2, CTA budget rules respected).
- Golden tests: fixture snapshot → expected mapping (checked in); every emitted candidate validates against the real zod schemas in-test.
- Body copy handling per the ratified OQ-W12-1 model (per-project, contract-owned): mark every text field `source: 'extracted'` in the mapping artifact so T12.4 can regenerate-or-keep according to the **target project's contract-declared copy-rights posture** (no global rights assumption baked in here).

## Deliver

Deterministic-enough mapping artifacts + gap reports from a snapshot, schema-valid by construction.

## Non-goals

- No store writes (T12.4). No new section types (gaps get REPORTED, not improvised). No CSS emission of any kind.

## Acceptance criteria

- Golden tests green; 100% of emitted candidates pass schema validation; gap report format documented in the plan-adjacent docs.
- Committed `T12.2: …`; no PR.
