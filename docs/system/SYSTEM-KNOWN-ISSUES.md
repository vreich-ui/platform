# System known issues — cross-repository defects confirmed from both sides

> Pins: `CMS_AGENT_SHA=44acb04a1f54281761ab3c34219913198d117950` · `PLATFORM_SHA=d5845dd6e855b434547430d6b0bb4d60b9f60a3c` · `PDF_TOOL_SHA=2c28a4b6430a9589b384dd634f08e9ace5c4e84b` · `KUGEL_DATA_SHA=d9723664521c97be87934874927e9e4603da68ba`.
> Only findings whose producer **and** consumer code were read are listed as S-nn. Repository-internal issues stay in each repository's `KNOWN_ISSUES.md` and are referenced by id. Severity = consequence for the system; "Fix belongs to" = the repository that must change first.

## A. Confirmed cross-repository defects

| ID | Sev | Finding | Evidence (both sides) | Failure scenario | Fix belongs to |
|---|---|---|---|---|---|
| S-01 | High (latent in prod) | **Fixed: platform #700.** The tracking→purchase join key can never match: `props.commerce_event_id` = checkout `metadata.event_id` (`randomUUID()`), commerce `event_id` = `deterministicUuid(session.id:type)` (webhook) or another `randomUUID()` (client kinds) | platform `create-checkout-session.ts:121`, `checkout-session-status.ts:33-40`, `stripe-webhook.ts:69-75,210,254`, `commerce-events.ts:123`; kugel-data `005:109-124`, `004:147-195` | every `purchase_rate`, `revenue_cents`, `v_sessions.purchased` is 0 regardless of sales; **remaining until kugel-data #13 merges: purchase joins stay at zero** | platform (mint the webhook's deterministic id at checkout creation, or carry `session.id` on the event); kugel-data documents the key |
| S-02 | High (latent) | **Fixed: kugel-data #13.** Purchase vocabulary: platform sends `kind: event.type` ∈ 8 types, none `purchase`; every sink revenue predicate is `kind = 'purchase'` | platform `commerce-events.ts:38-47,187`; kugel-data `tracking-sink-stats.ts:97`, `005:119-121`, seed `scripts/seed-rollups.mjs:195` | admin "Purchases" and `daily[].purchases` are 0 even with orders; independent of S-01 | one shared constant: platform emits `purchase` (or kugel-data filters `checkout_completed`) — decide once |
| S-03 | Medium | `node_strategy.strategy/intent` are null for every export published since the `private.*` strip (2026-08-31 ruling, in effect for exports ≥ 2026-09-03) | platform `materializers/shared.ts:132`, `tracking-dims-push.mjs:96-97`, exports (0 `private` nodes in the five newest); kugel-data `tracking-sink-dims.ts:81-97` upsert `COALESCE` keeps only older values | the "engagement × strategy" dimension is dead for all new content; the W21 strategy learner has nothing to group by | platform (export `node.strategy`/`node.intent` as public neutral slugs, or push dims from the store) |
| S-04 | Medium | `by=strategy` grain: CMS-Agent requests it and treats only a 503 as "not deployed"; kugel-data answers **400**; the "migration 008" CMS-Agent names does not exist on GitHub. **S-04b:** `strategy-review` reads `funnel_stage/stage/funnel`, `topic/…`, `n/count/event_count` from `by=object` rows that carry none of them | CMS-Agent `strategyLearning.ts:543-575`, `strategyReview.ts:163-180,699-717`; kugel-data `_shared/rollups.ts:19-36,91-94`, migrations 001–005 | `job:strategy-learning` exits non-zero on every run; `job:strategy-review` always `no_rows` | kugel-data (add the grain with a declared row shape) **and** CMS-Agent (treat 400 as grain-absent until then); the row contract must be written down first; **stopped — needs a decision:** CMS-Agent's `fetchRollupRows` deliberately never surfaces the sink's response body on a non-2xx, and matching the sink's `by must be one of: object, producer` message requires reversing that stated invariant |
| S-05 | High (for learning) | **Fixed: CMS-Agent #270.** Producer context reaches the sink only from the platform-tenant hook and from plugin LLMs. CMS-Agent's dr-lurie hook sends no `producer`; drlurie is the only tracked tenant. Plugin rows carry `node_id = 'plugin:<actor>'`, `run_id = 'plugin_<actor>_<request>'` — labels, not CMS-Agent nodes/runs | CMS-Agent `drLurie/hooks.ts:162` vs `platform/hooks.ts:157-165`; platform `producerContextSchema`, `tracking-dims-push.mjs:86`, `render-skill.ts:313`; kugel-data `v_producer_window`; CMS-Agent `trackingIngest.ts:254-267` keys feedback by `(node_id, run_id)` | `feedback.ingest_tracking` never produces an outcome for a conductor-authored article; outcomes it does produce are keyed to non-nodes | CMS-Agent (send `producer` from the dr-lurie hook — `producerContextForPublish` already computes it); platform (stamp a stable plugin producer server-side instead of trusting the LLM) |
| S-06 | Medium | Platform assumes the tenant object id equals the editorial request id (`recordObject(… object_id: requestId)`, `mintWorkspaceRequestId` bumps on existence); CMS-Agent publishes under `run.requestId` only on the content-item-shell path, otherwise under `artifact_plan`'s or a conductor-minted `req_conductor_…` id | platform `requests/sweep.ts:395-413`, `agent/tools.ts:1069-1083`; CMS-Agent `publishRequestId.ts` rule 3, `runContext.ts:95-133`, `artifactMaterialization.ts:772-779`, `contentItemShell.ts:81-98`; exports show both id forms | a request doc's `object.object_id` names a shell or a non-existent object; "Open object" links and publication evidence point at the wrong record | CMS-Agent (make the shell path unconditional for live runs, or return the published `objectId` at top level) and platform (record `object_id` from run receipts, not from the request id) |
| S-07 | Medium | **Fixed (partial): CMS-Agent #270.** Platform calls **nine** CMS-Agent tools with the genesis-minted tenant chat bearer that are outside its 11-tool scope: `node_get_latest_output` (`requests/publication-outputs.ts:57`); `workflow_retry_node`, `workflow_set_node_budget_override`, `workspace_update_node_model_config` (`requests/budget-override.ts:47-60`, reached from `admin-request-activity.ts`); `workflow_cancel_run` (`functions/admin-requests.ts:107`); and, since PR #694, the admin Insights tab's `feedback_list`, `playbook_get`, `optimizer_status`, `learning_list_observations` (`server/lib/analytics-insights.ts:184,242,301,343`, "one client per site binding"). (`cms-agent-client.ts:908` also carries a `nodeExecute` wrapper with no caller — `node_execute` must never reach a tenant bearer per CMS-Agent ruling R1.) CMS-Agent refuses each before dispatch (`isScopedMessageAllowed`); the daily reconciler re-narrows any hand-widened bearer. Platform degrades every refusal (compact-view fallback; Insights sections render empty; budget/retry surface as a failed action) — `analytics-insights.ts`'s header says the four calls were "verified live against CMS-Agent on 2026-09-05", which is possible only with a hand-widened or full bearer; CMS-Agent PR #268 (`44acb04`) likewise reports the Insights tab working in production and failing only on deploy previews (empty bearer) — consistent with a production credential that is **not** the 11-tool bearer, which no repository can confirm (UNKNOWN 9) | CMS-Agent `siteGenesis.ts:123-138`, `docs/site-credential-scope-lock.json` (`toolCount: 11`), `mcpEndpoint.ts:98-123`; platform `site-binding.ts:87-88` (one CMS-Agent credential), `publication-outputs.ts:57-62`, `publication-evidence.ts:155-157`, `budget-override.ts:47-60`, `analytics-insights.ts:184-343` | request "done/published" evidence is derived from node status + warning prefixes instead of receipts (`article_path`, `commit_sha`, `deployedSha` never reach the request doc); "raise budget and retry" cannot work; the Insights tab is structurally empty on a genesis-minted bearer — and `learning_list_observations` is additionally broken on CMS-Agent's side once any conversation ledger exists (CMS-Agent C-1) | CMS-Agent (add the read tools to `SITE_CLIENT_MANAGER_TOOLS` + `scope:update` + reconciler `--apply`; the workspace-mutating ones are a reviewed widening) or platform (drop the calls) |
| S-08 | High (for learning) | Page-level event kinds (`pageview`, `engagement`, `scroll_depth`, `cta_click`, section kinds) name the **page** object; only node kinds name the article. The sink's per-object denominators use `pageviews`/`exposures` | platform `loader/index.ts:68-79`, `loader/core.ts:156-173`, drlurie `tracking.json` `defaults.content_item`; kugel-data `005:132-190` `COALESCE(NULLIF(exposures,0), pageviews)`; PROD-OBSERVED `top_objects` are all `page_*` | per-article `pageviews`, `cta_ctr`, `buy_click_rate`, `purchase_rate` are structurally 0; `tracking:engagement.v1` carries information only in `sessions`, `completion_rate`, `p75_dwell_ms` | platform (emit an article-level landing/view event, or the sink adopts sessions as the denominator) — decide the contract first |
| S-09 | Medium | Experiments are now produced on both sides but the two halves do not join. Platform emits `exposure {experiment_id, variant_id}` once per page-load (`loader/core.ts:451-456`, `loader/index.ts:111-118`) and serves arms from the `variant-serve` edge function; kugel-data reads only `props->>'variant_id'` (`004:48-59`), keys every arm by the **event's `object_id`**, and the synthetic `'control'` label still stands for never-exposed sessions (`005:43-61`). At `PLATFORM_SHA` every tenant ships `experiments: []`, so nothing is live | platform `tracking-config-v1.ts` (`experiments[]`), `netlify/edge-functions/variant-serve.ts`, `sites/*/public/_trk/experiments.json` (`{}`); kugel-data `004`, `005`, `_shared/experiment.ts:305-365` | activating an experiment produces exposures the sink attributes to the page shell (S-25) and weights the build cannot read (S-24); kugel-data KI-03 (zero-exposure arms) remains the sink's own guard | platform + kugel-data — agree the join key (`experiment_id` = control content_item id) before any tenant sets `experiments[]` |
| S-10 | Low | Two definitions of "completion": platform emits `completion` (first impression of `last_node_id`); kugel-data derives it from `node_impression` on the highest-position `node_strategy` row and never reads `completion` | platform `loader/core.ts:350`; kugel-data `_shared/stats.ts`, views `is_last_node` | two numbers for one metric; the sink's depends on a dims push having run | kugel-data (read the emitted kind) |
| S-11 | Low | **Fixed: CMS-Agent #270.** Idempotent-replay marker: platform returns `replayed_from_idempotency_key: true` (boolean); CMS-Agent tests it as a non-empty string or `idempotent_replay === true` | platform `idempotency-store.ts:98-101`; CMS-Agent `releaseExecution.ts:238` | a replayed release is recorded as a fresh one in release notes; ledger still prevents a second hook | CMS-Agent (accept `=== true`) |
| S-12 | Low | **Fixed: kugel-data #13 (KI-27).** `object_version.surface/attribution` are pushed and dropped | platform `tracking-dims-push.mjs:60-75`; kugel-data `002` columns, `normalizeObjectVersion` | "do plugin-written articles perform differently?" is unanswerable from the sink | kugel-data (two nullable columns + two lines) |
| S-13 | Medium | No revision on events; `v_producer_window` attributes all history to the latest published version; a republish rewrites past attribution | platform `objectRefSchema` (no `version`), export `__generated.record_version` available at render; kugel-data `004:23-25`, `005:194-296` | outcomes ingested for `run_old` are not reproducible after a republish | platform (stamp `object.version` from the rendered export) + kugel-data (column + view) |
| S-14 | Medium | The three W21 CMS-Agent jobs and their env (`TRACKING_SINK_URL/TOKEN/PROJECT_ID`, `EDITORIAL_STRATEGY_*`, `STRATEGY_REVIEW_AUTOPATCH`) exist in code only; no Cloud Run job, schedule or env in any deploy artifact (genesis expects `TRACKING_SINK_URL/TOKEN` on the service env to install them on new sites — `siteGenesis.ts:266-272` — so those two may be hand-set); `conductor-run` image not synced either (CMS-Agent C-10) | CMS-Agent `cloudbuild.deploy.yaml:44,101-102`, `scripts/deploy-*.sh`, `package.json` scripts, `capture/siteGenesis.ts:266-272` | the learning path documented in PR #266 cannot run in production as committed | CMS-Agent (deploy artifacts + `_EXECUTOR_JOBS`) |
| S-15 | Low | Grant fields `grantVersion` and `limits.overBudget` are minted and ignored; pdf-tool's warn-vs-block for media is therefore never inherited from the tenant policy | platform `pdf-tool-storage-grant.ts:44-66`; pdf-tool `storage-grant.ts:152-162` | a tenant with `overBudget:'block'` still gets warn-only images from pdf-tool jobs | pdf-tool (read it) or platform (stop declaring it) |
| S-16 | Medium (risk) | Two implementations of the artifact layout, index keys and reference shape (pdf-tool and platform); platform's allowlist rejects unknown fields; the `filename` addition already broke every reference for four days | SYSTEM-ARTIFACTS.md §6 | the next pdf-tool field addition repeats the incident | platform (import or generate the field list from a shared fixture; a contract test) |
| S-17 | Medium | Release targets branch HEAD and nothing records that a release is owed; CMS-Agent's `release_executor` passes no `commit`, so a run's release ships every dark export on the shared branch (all four tenants) and a run that publishes but fails to release leaves its export dark until someone else releases | platform `production-release.ts:98-118,236`, CMS_INTEGRATION #3; CMS-Agent `releaseExecution.ts:287` | cross-tenant coupling through one repo branch; unattributable go-lives | platform (record `release_owed` on publish; accept `commit` from receipts) + CMS-Agent (pass `receipts.commitSha`) |
| S-18 | Medium | Duplicate side effects on retry: a CMS-Agent claim expiry re-dispatches `artifact_materializer` or `publish_executor`; the platform idempotency store protects `create_agent_artifact_job` and `release_to_production` only when a key is sent (artifact: yes; release: yes; `object_publish`: **no key**); pdf-tool creates a new job per call | CMS-Agent K-D1, `artifactMaterialization.ts:554`, hooks; platform `mcp.ts:1404-1415`; pdf-tool AI_CONTEXT "requestId is an idempotency key only for capture" | a second publish attempt re-runs six verbs; safe only because materialization is content-addressed and `object_checkout` locks | CMS-Agent (send `idempotency_key` on `object_publish`; heartbeat claims) |
| S-19 | Medium | One `TRACKING_SINK_TOKEN` for all writers and `/rollups` readers; `project_id` unchecked; `/stats`/`/weights` unauthenticated and enumerable | kugel-data KI-13/14; platform and CMS-Agent env usage | any tenant env leak exposes every tenant's analytics | kugel-data (per-project tokens or allow-list) |
| S-20 | Low | `materializationProof` is stored by nobody and is forgeable when pdf-tool falls back to `AGENT_RUN_TOKEN`; platform's `verified:true` is the only trust signal it passes on | SYSTEM-ARTIFACTS.md §4; pdf-tool `artifact-attestation.ts:54-70` | later re-verification is impossible; which secret `pdf-x` has is UNKNOWN | pdf-tool (require a dedicated secret in production) |
| S-21 | Low (doc-critical) | Four "project id" domains for one tenant: CMS-Agent project id `dr-lurie`; tracking `project_id` `drlurie`; tenant object `site_drlurie`; pdf-tool grant `projectId` (env or slug) — plus `CMS_AGENT_PROJECT_ID` on the tenant | CMS-Agent `defaultProjects.ts`; platform `site-identity.ts:239-240`, `track-ingest.ts:234-239`; kugel-data partition key | joins across systems require a lookup table that exists in nobody's code | document (done, SYSTEM-IDENTIFIERS.md); a shared identity record is FUTURE |
| S-22 | Medium | **Fixed: platform #700.** `TRACKING_SINK_URL` has two meanings in one variable: the event relay POSTs to it **verbatim** (`track-ingest.ts:262`); the readers strip trailing slashes and append `/stats`, `/dims`, `/commerce`, `/link`, `/rollups`, `/weights` (`own-tracker-stats.ts:79`, `own-tracker-rollups.ts`, `member-link.ts:28`, `tracking-dims-push.mjs:177`, `commerce-events.ts:206`, `scripts/lib/tracking-experiments.mjs:186`; CMS-Agent `trackingIngest.ts:204`) — which works only because kugel-data nests every route under `/api/tracking-sink` and the value is the full relay URL; but the new raw-export reader appends **`/api/tracking-sink/export`** (`own-tracker-stats.ts:153`) and its tests use a site-root value (`https://sink.example.test`), so with the value every other caller needs the export URL becomes `…/api/tracking-sink/api/tracking-sink/export` | platform callers above; kugel-data `config.path` per function | the export download is a 404 under the relay-URL convention and every other reader breaks under the site-root convention; there is no value that satisfies both | platform (one convention: base URL + fixed paths everywhere) |

| S-23 | Medium | **Fixed (scoped): kugel-data #13.** `/stats` request contract diverges: platform sends `project_id&from&to[&exclude_test=1&country&source&object_id]` (`own-tracker-stats.ts:60-80`, R6.2) and "does not validate strictly"; kugel-data at `KUGEL_DATA_SHA` reads `project_id` and `days` only (`tracking-sink-stats.ts:46-52`, `clampDays`) and ignores every other parameter. `/api/tracking-sink/export` (R11.2) does not exist on kugel-data (no function, no `config.path`) — platform degrades a 404 to "not available on this sink yet" by design | platform `own-tracker-stats.ts:5-30,60-80,116-153`; kugel-data `tracking-sink-stats.ts`, function list | every own-tracker window (90 d, custom) and every D7 filter silently returns the sink's default 7-day, unfiltered aggregate while the UI labels it as the requested window; the payload's `days: 7` is the only tell | kugel-data (`runner/w21-r116` is the intended fix; until it lands, platform should refuse windows the sink cannot honour) |
| S-24 | Medium | **Fixed: platform #700.** `/weights` response envelope mismatch: kugel-data returns `{project_id, experiments: {object_id: {variant_id: weight}}}` (`tracking-sink-weights.ts:34-40`); both platform readers expect `body.weights` and otherwise use the **whole body** as the map (`scripts/lib/tracking-experiments.mjs:198-199` at build; `server/lib/own-tracker-rollups.ts:147` for the admin Variants workspace; the build's test fixture is `{weights: …}`), so `weights[controlId]` is always undefined | platform `scripts/lib/tracking-experiments.mjs:169-202`, `own-tracker-rollups.ts:113-155`, `tests/scripts/tracking-experiments.test.mjs:104`; kugel-data `tracking-sink-weights.ts` | Thompson-sampled weights are computed nightly and never applied; every experiment runs at equal weights forever, silently (`normalizeWeights` fallback); **remaining: S-25** (the sink keys arms by the page shell's `object_id`) | platform (read `body.experiments`) — or kugel-data renames; one of the two, plus a contract test |
| S-25 | Medium | Experiment join key: platform keys experiments by the **control content_item id** (`experiments[].object_id`, `buildExperimentMap`, `experiment_id` on the event); kugel-data never reads `experiment_id` and keys arms by the exposure event's `object.object_id`, which the loader sets to the first `data-cms-object-id` on the page, preferring `page_*` (`loader/index.ts:68-79`) — on an article route with any `page_article` section that is `page_article`, the same condition that makes S-08 attach pageviews to the page shell (PROD-OBSERVED: top objects are all `page_*`) | platform `loader/index.ts:68-79,111-118`, `loader/core.ts:451-456`, `scripts/lib/tracking-experiments.mjs:84-111`; kugel-data `004:44-62`, `_shared/experiment.ts:320-364`, `experiment_weights` PK | `experiment_weights.object_id` = `page_article`, never a content_item id; even with S-24 fixed the build's lookup by control id finds nothing; arm rollups for two experiments on the same route collide under one `object_id` | platform + kugel-data — sink adopts `props.experiment_id` as the arm key (or platform stamps `object.object_id` = control id on `exposure`); decide once, with S-08 |
| S-26 | High | **Fixed: platform #700, CMS-Agent #270 (K-M9).** A tenant-scoped chat bearer acts on **any run by `runId`**: `isScopedMessageAllowed` checks the tool allowlist and then `requestedProject(arguments)` — a call with no `projectId`/`project_id` passes (`mcpEndpoint.ts:98-123`); `publishRun` uses `input.projectId ?? run.projectId` without checking the two agree (`publisher.ts:250`). Platform's approve-and-publish tool calls `workflow_get_run {runId}` and `workflow_set_operator_publish_decision {runId, decision}` **without** `projectId` (`agent/tools.ts:1271,1347-1349`) and `workflow_publish_run {runId, projectId: <own project>}` (`tools.ts:1356-1358`) | CMS-Agent `mcpEndpoint.ts:98-123`, `publisher.ts:250`, `docs/KNOWN_ISSUES.md` K-M9 (reproduced: a `platform`-scoped bearer reads and approves a `dr-lurie` run); platform `agent/tools.ts:1271-1378` | any holder of one tenant's `CMS_AGENT_MCP_TOKEN` (each tenant's Netlify env) can read, approve and publish another tenant's run given its `runId`; through platform's own tool the foreign run's body is published **into the caller's site** via the caller's hooks — the tenant boundary in SYSTEM-SECURITY-BOUNDARIES.md SEC-1 holds only for calls that name a project | CMS-Agent (resolve the run first; enforce `run.projectId ∈ policy.projects` for every run-addressed tool; refuse `input.projectId !== run.projectId`) — platform should also pass `projectId` on every run-addressed call |
## B. Documentation contradictions across repositories (no code change implied)

| ID | Where | Contradiction | Resolution |
|---|---|---|---|
| S-DOC-01 | CMS-Agent `docs/projects/dr-lurie-agent-publishing-policy.md` §8.2 (copy of platform's policy) | `approved:true` authorises a publish | code on both sides ignores/omits it; retire the paragraph |
| S-DOC-02 | platform PR #695 as first pushed (15cc28c) | cited `docs/generated/INVENTORY.md` and `scripts/docs/inventory.mjs` before they existed | **closed** — both, plus `tests/scripts/docs-inventory-fresh.test.mjs`, are on `main` at `PLATFORM_SHA` |
| S-DOC-03 | CMS-Agent `docs/AI_CONTEXT.md` (still at `0d1dfa4`, after PR #267) | silent on the W21 tracking consumer, `provenance.source:'tracking'`, unflagged strategy promotion, engagement evidence in the reflector prompt | update §2 facts and §1 job list; #267 added §7e (external contracts) but no tracking-consumer facts |
| S-DOC-04 | CMS-Agent `docs/ARCHITECTURE.md` §2 diagram and (since #267) §9 external-contracts table | draws `MCP --> PdfTool` and `Jobs --> PdfTool`; §9 cites `artifactMaterialization.ts`/`captureEngine.ts` as the PDF-Tool contract | both modules call the run's **tenant** `/mcp` (`ProjectMcpAdapter`); no job/capture/template call goes to pdf-tool directly; only an unused read-only project. Redraw the edge through the tenant and move the §9 row's evidence to `projects/pdfTool/definition.ts` |
| S-DOC-05 | kugel-data `docs/KNOWN_ISSUES.md` KI-28 (unchanged through correction passes 4B/4C, at `KUGEL_DATA_SHA`) | plugin producer rows have `run_id NULL` | the five producer-carrying drlurie exports at `PLATFORM_SHA` all carry `run_id` (`plugin_<actor>_<request id>`, one with a `_media_revision` suffix; actors `plugin:claude`, `plugin:openai-agent`, `plugin:openai-gpt`); production dims rows UNKNOWN — label the claim PROD-OBSERVED-at-a-time or drop it |
| S-DOC-06 | platform `TRACKING_ARCHITECTURE.md` §10 | "nothing moves tracking into CMS-Agent" | true of platform; false at system level (CMS-Agent consumer exists, unscheduled) |
| S-DOC-07 | pdf-tool `ARTIFACT_CONTRACT.md` §C | "no transformation between A and what a project receives" | platform re-validates with a strict allowlist and adds `public_path`/`verified`; CMS-Agent projects again |

## C. Legacy components that remain callable (cross-repo significance: three extra authenticated doors)

| System | Component | Auth | Caller at the pins | Action |
|---|---|---|---|---|
| platform | `run-publisher-agent.ts` (Agents SDK runtime writing `content_item`s) on four tenants | `x-publish-key` | none (a dead ChatKit widget) | remove |
| platform | `save_artifact` tool + `save-artifact.ts` (base64 single shot) | `x-publish-key` via `/mcp` | CMS-Agent dr-lurie allowlist names it; no call site in engine code | keep internal-only or remove |
| platform | `admin-traffic` shim, `netlify/lib/{admin-auth,netlify-deploys}.ts` shims | — | `verify-article-images.ts` imports the shims | fold in |
| CMS-Agent | Netlify functions `agent`, `mcp`, `oauth-*` (502 since 2026-08-14), `/api/agent` scaffold | `AGENT_API_TOKEN`, `MCP_API_TOKEN` | none in production | remove all but `session` |
| CMS-Agent | `pdf-tool` and `monetizer` project rows (`PDF_TOOL_MCP_*`, `MONETIZER_MCP_*` — the latter absent from deploy) | Secret Manager | none for jobs | mark read-only diagnostics or remove |
| pdf-tool | `agent-artifact-job`, `agent-artifact-job-status` HTTP (bypass budget/routing/auto-fail) | bearer | none | delete or wrap |

## D. UNKNOWN — cannot be settled from the four repositories

1. Which branch `publish_executor` takes in production (`workspace_get_node {"id":"publish_executor"}`).
2. Whether any tenant has `publishingPolicy.autonomyMode: "autonomous"` on its CMS-Agent project record.
3. What kind of credential `<CLIENT>_MCP_TOKEN` is on each tenant (agent key vs shared token).
4. Whether `TRACKING_SINK_*` / `EDITORIAL_STRATEGY_*` are set anywhere for CMS-Agent, and whether the W21 jobs are run by hand.
5. `PDF_TOOL_PROJECT_ID`, `TRACKING_PROJECT_ID`, `CMS_AGENT_PROJECT_ID` values per tenant; whether they agree with CMS-Agent's project ids.
6. Which attestation secret `pdf-x` has (forgeable proofs or not).
7. Production `producer` dims rows (kugel-data KI-28's NULL `run_id`).
8. The content of kugel-data branch `runner/w21-r116` (migrations 006–008, `/export`, `/stats` rewrite) that both CMS-Agent (`by=strategy`) and platform `main` (since #694) code against.
9. Which bearer the drlurie production site holds as `CMS_AGENT_MCP_TOKEN`: `analytics-insights.ts`'s "verified live 2026-09-05" note and CMS-Agent #268's "production was fine" both imply the Insights calls succeed there, which the genesis-minted 11-tool bearer cannot do (S-07); either the reconciler has never rotated that site or the credential is a full/static token — decidable only from the Netlify env and CMS-Agent's managed-bearer registry.
10. Whether `TRACKING_SINK_URL`/`TRACKING_SINK_TOKEN` exist as team-level Netlify env (platform R8.1 assumes it; CMS-Agent genesis still reads them from its own process env — C-11).
