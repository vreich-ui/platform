# System conflict ledger — where the four repository audits disagree, and what the code says

> Pins: `CMS_AGENT_SHA=0d1dfa43f827aae3b5b9ffdfaf71868a8076ffd3` · `PLATFORM_SHA=99fb36993f156fbdc6a91cb717e890ca2c2adfef` · `PDF_TOOL_SHA=2c28a4b6430a9589b384dd634f08e9ace5c4e84b` · `KUGEL_DATA_SHA=6c9c7129467bdb947ce76c0f52876b135aa24a61`.
> Inputs compared: CMS-Agent `docs/*` (written at `40424c4`, one commit before the pin), platform `docs/*` (at the pin, plus PR #695 corrections), pdf-tool PR #78 `docs/*` (at the pin), kugel-data PR #9 `docs/*` (at the pin). Every row was resolved by reading code on both sides of the boundary; nothing here averages two documents.

Columns: **Domain** · **Disagreement** (who said what) · **Code says** · **Label** · **Consequence for the system docs**.

| # | Domain | Disagreement | Code says | Label | Consequence |
|---|---|---|---|---|---|
| L-01 | Learning / tracking consumer | platform `TRACKING_ARCHITECTURE.md` §10: "tracking data reaches a human dashboard and stops"; `/rollups` has "zero callers". kugel-data `AI_CONTEXT.md`: "CMS-Agent reads `/rollups?by=producer`". CMS-Agent `AI_CONTEXT.md` (at `40424c4`): silent. | CMS-Agent at the pin has one sink client, `fetchRollupRows` (`src/agent/improvement/trackingIngest.ts:193-222`), used by `feedback.ingest_tracking`, `job:strategy-learning`, `job:strategy-review`. No deploy artifact schedules any of the three jobs (`cloudbuild.deploy.yaml:44` names only `continuation-tick`; no `TRACKING_SINK_*` in any deploy file). Platform code never calls CMS-Agent feedback tools. | VERIFIED-CONSUMER-ONLY (consumer code exists; runtime scheduling UNKNOWN) | Platform's sentence is true of platform; the system-level statement is "a consumer exists in CMS-Agent, is unscheduled, and its joins are broken" — see SYSTEM-TRACKING-AND-ATTRIBUTION.md §7. |
| L-02 | `/rollups?by=strategy` | CMS-Agent `strategyLearning.ts:543-546` expects a 503 "until kugel-data migration 008 has run". kugel-data audit: "in flight branch `runner/w21-r116` (migrations 006–008)". | kugel-data `_shared/rollups.ts:19-36` accepts only `object`/`producer` and answers **400** `"by must be one of: object, producer"`; migrations end at 005; no such branch exists on GitHub. CMS-Agent treats only 503 as "grain unavailable" (`:568`), so a 400 becomes `errors[]` and `job:strategy-learning` exits non-zero when it produced no observations (`strategyLearningJob.ts:107`). | CONTRADICTED | Recorded as SYSTEM-KNOWN-ISSUES S-04. The strategy grain is a future contract, not a current one. |
| L-03 | Request id = object id | platform `requests/store.ts:614-640` and `sweep.ts:395-413` record `object_id = requestId` ("the shell is created under exactly that id"). CMS-Agent `publishRequestId.ts` rule 3 and `runContext.ts:95-133`: "`run.requestId` is never a source … the platform/workspace join key"; the publish id is `artifact_plan`'s authored id, else `run.publishRequestId`, else a conductor-minted `req_conductor_…`. | Both are right under different conditions: `artifactMaterialization.ts:772-779` makes the **content-item shell's** id (= `run.requestId`) win over the planner's id **when a shell exists**; the shell exists only for live runs whose `requestId` matches the dialect pattern and whose `artifact_materializer` node dispatched (`executor.ts:1895-1912`, `contentItemShell.ts:81-96`). Text-only runs (artifact nodes skipped) and pattern-invalid ids publish under a different id. Committed exports show both outcomes (`req_agent_fruit_…` with 77 nodes; `req_conductor_ipwrnx_20260829_01`). | CONTRADICTED (conditional equality, neither audit states the condition) | SYSTEM-IDENTIFIERS.md §3 states the rule; SYSTEM-KNOWN-ISSUES S-06. |
| L-04 | `autonomyMode` | CMS-Agent: project-record `publishingPolicy.autonomyMode` is the authority; which tenants set it is UNKNOWN from the repo. platform: `lib/publishing-policy.ts` "never configured … permanently `operator-gated`", "the CMS-Agent-side autonomyMode has no counterpart here". | Two independent switches with the same name and no synchronisation: CMS-Agent's decides whether **conductor runs** publish without an operator decision (`publishDecision.ts`); platform's decides whether the **admin chat** may satisfy an `ask` floor without a human (`agent/registry.ts:13`) and is always `operator-gated`. Neither reads the other. | VERIFIED-BOTH-SIDES (duplicated concept) | Authority matrix marks "publishing autonomy" AMBIGUOUS; SYSTEM-PUBLISHING.md §5 lists all three autonomy switches. |
| L-05 | `approved:true` as publish authority | CMS-Agent D-8: platform's dr-lurie policy §8.2 (copy in CMS-Agent `docs/projects/`) still says `approved:true` authorises. | Platform chat deliberately omits `approved` (`server/lib/agent/tools.ts:1355-1375`); CMS-Agent ignores it (`publishDecision.ts`). Code agrees; only the copied policy prose is stale. | VERIFIED-BOTH-SIDES | Doc-only drift, recorded S-DOC-01. |
| L-06 | "Project-native ArtifactReference" | pdf-tool `ARTIFACT_CONTRACT.md` §C: layer C ≡ A, "no transformation between A and what a project receives". CMS-Agent: refs are "opaque strings". | Platform re-declares the type (`server/lib/artifacts.ts:32-56`) with a **strict 13-key allowlist** (`:336-351`) and rejects any other key; it once rejected every reference for four days when pdf-tool added `filename` (comment at `:40-50`). Platform then adds `public_path` + `verified` for its callers. CMS-Agent stores a tolerant projection `{...ref, blobKey}` plus `publicPath` and its own `verification` (`artifactMaterialization.ts:477-539`). | CONTRADICTED (an adapter-by-rejection exists) | SYSTEM-ARTIFACTS.md §2 defines three consumer representations, not one. |
| L-07 | `materializationProof` | pdf-tool recommends consumers persist it (layer G). | Platform never stores it — extracted to an internal field, stripped from every response and log (`pdf-tool-client.ts:13,55-68,136-139`), forwarded once to `verify-agent-artifact`. CMS-Agent strips it as credential-shaped (`captureEngine.ts:273-286`). | VERIFIED-BOTH-SIDES | The proof is a per-call attestation, never a stored fact; SYSTEM-ARTIFACTS.md §4. |
| L-08 | CMS-Agent → pdf-tool transport | CMS-Agent `ARCHITECTURE.md` §2 diagram draws `MCP --> PdfTool` and lists pdf-tool as a registered project. | Every artifact, template and capture call goes to the **run's tenant `/mcp`** through `ProjectMcpAdapter` (`artifactMaterialization.ts:760-762`, `captureEngine.ts:249-257`, `pdfTemplateEngine.ts:275-302`); the `pdf-tool` project is read-only and its two capture rows are annotated UNUSED (`projects/pdfTool/definition.ts:7-41`). CMS-Agent never mints or forwards a storage grant. | CONTRADICTED (docs vs code) | System context diagram has no CMS-Agent→pdf-tool job edge; only a dormant read-tool edge. |
| L-09 | `producer` on published content | platform: "3 of 27 committed articles carry a producer context". kugel-data KI-28: plugin producer rows have `run_id = NULL`, `node_id = 'plugin:claude'`. | At `PLATFORM_SHA` the three producer-carrying exports carry **all four** fields (e.g. `req_plugin_azelaic_acid_20260904_01`: `run_id: plugin_claude_req_plugin_azelaic_acid_20260904_01`); the plugin skill text instructs the LLM to author them (`plugin/render-skill.ts:313`). CMS-Agent's dr-lurie hook sends **no** `producer` on `object_publish` (`projects/drLurie/hooks.ts:162`); only the platform-tenant hook does (`projects/platform/hooks.ts:157-165`). | KI-28's NULL `run_id`: not reproducible from committed exports (production dims rows UNKNOWN). No-producer-from-conductor: VERIFIED-BOTH-SIDES | S-05: CMS-Agent's own drlurie content is invisible to the producer grain; plugin producer ids are agent-authored labels. |
| L-10 | Article pageviews | platform correlation table: "event → object: yes". kugel-data KI-05: articles never receive `pageview` (attached to `page_article`), so per-object rates are structurally 0. | Platform loader attaches the page object to `pageview`/section/page-level kinds and the article object only to node kinds (`loader/index.ts:68-79`, `loader/core.ts:156-173`); kugel-data denominators use `COALESCE(NULLIF(exposures,0), pageviews)` (`005:132-190`). | CONTRADICTED (join exists; denominator does not) | S-08. |
| L-11 | Storage grant `limits` | platform grant comment: "warn-vs-block … pdf-tool honors". | pdf-tool `parseStorageGrant` reads only `maxImageBytes` and `preferredImageFormat` (`storage-grant.ts:152-162`); `overBudget` is dropped silently. | VERIFIED-PRODUCER-ONLY | Contract registry row "storage grant": `limits.overBudget` is not consumed. |
| L-12 | Idempotent-replay marker | CMS-Agent reads `replayed_from_idempotency_key` as a **string** or `idempotent_replay === true` (`releaseExecution.ts:238`). | Platform writes `replayed_from_idempotency_key: true` (boolean) and `idempotency_key: <key>` (`idempotency-store.ts:98-101`). CMS-Agent therefore never recognises a replayed `release_to_production`; effect limited to one release note (`:377`). | CONTRADICTED | S-11 (low). |
| L-13 | `schema` on tracking events | platform: `tracking_event.v1` is a strict literal on every event. kugel-data KI-16: the field is dropped at ingest. | `tracking-sink.ts:47` lists `schema` as known and never stores it; no column exists. | VERIFIED-PRODUCER-ONLY | Contract registry: wire version enforced by the producer only; the store cannot distinguish a future v2. |
| L-14 | `exposure` / `variant_id` | kugel-data views require them; the platform audit (at `420afbd`) found neither; platform `main` since #694 emits `exposure {experiment_id, variant_id}` and serves arms at the edge. | Both sides exist at the pins but key the arm differently (platform: control content_item id via `experiment_id`; kugel-data: the event's `object_id`, `experiment_id` unread) and the weights envelope does not match (`experiments` vs `weights`). Every tenant ships `experiments: []`. | CONTRADICTED (producer and consumer disagree on the join key and the envelope) | S-09, S-24, S-25; experiments stay FUTURE in SYSTEM-FUTURE-EXTENSIONS.md until the key is agreed. |
| L-15 | `/stats` query contract | platform `main` (since #694): `?project_id&from&to[&exclude_test&country&source&object_id]` plus a new `/api/tracking-sink/export` reader. kugel-data at the pin: `project_id&days` only, no `/export`. | Platform codes against a kugel-data branch (`runner/w21-r116`) that is not on GitHub; the deployed sink ignores every new parameter and answers its 7-day default. | CONTRADICTED | S-23; revalidation trigger in SYSTEM-OPERATIONS.md §6. |
| L-16 | `x-trk-test` | platform: header set by drills only; "one-sided". kugel-data: NOT FOUND IN CODE. | Agree. | VERIFIED-PRODUCER-ONLY | One-sided contract; listed in registry as "no consumer". |
| L-17 | Generated inventory | platform PR #695 `AI_CONTEXT.md:26,72,90` and `ARCHITECTURE.md:89,213` cite `docs/generated/INVENTORY.md` and `scripts/docs/inventory.mjs`, "freshness-tested". | Neither path exists at the pin nor on the corrections branch; `npm test` has no such test. | CONTRADICTED (docs vs repo) | S-DOC-02; the drift tooling added by this change set is the first generated cross-repo reference. |
| L-18 | "Netlify Blobs" | CMS-Agent docs: Netlify Blobs is a LEGACY, migrated-off backend. platform and pdf-tool docs: Netlify Blobs is the canonical store. | Both true for their own repos; CMS-Agent state is GCS, tenant content and artifact bytes are Netlify Blobs. | VERIFIED-BOTH-SIDES | Glossary note: "Blobs" without a qualifier is ambiguous — always name the site. |
| L-19 | Completion | platform emits a `completion` event kind (`loader/core.ts:350`). kugel-data derives completion from `node_impression` on the highest-position `node_strategy` row and never reads `completion` (`_shared/stats.ts`, views). | Two definitions of one metric. | VERIFIED-BOTH-SIDES (semantic split) | S-10. |
| L-20 | Legacy callable surfaces | Each audit lists its own: CMS-Agent Netlify `agent`/`mcp` functions; platform `run-publisher-agent.ts`, `save_artifact`; pdf-tool `agent-artifact-job*` HTTP. | All still deployed and authenticated; none has a live cross-repo caller at the pins (grep across the four repos). | VERIFIED-BOTH-SIDES | SYSTEM-KNOWN-ISSUES §Legacy. |
| L-21 | Commerce join key and purchase kind | platform #8/#9 and kugel-data KI-01/KI-02 agree. | `create-checkout-session.ts:121` random; `stripe-webhook.ts:210` deterministic; `checkout-session-status.ts:33-40` echoes the random one; sink joins `ce.event_id::text = props->>'commerce_event_id'` and filters `kind = 'purchase'` while platform sends `kind: event.type` (`commerce-events.ts:187`). | VERIFIED-BOTH-SIDES | S-01, S-02 — carried into system docs because both sides were re-read. |
| L-22 | `node_strategy.strategy/intent` | platform #11 and kugel-data KI-08 agree. | `materializers/shared.ts:132` strips `private`; `tracking-dims-push.mjs:96-97` reads `node.private.*`; exports ≥ 2026-09-03 carry no `private` node. | VERIFIED-BOTH-SIDES | S-03. |
| L-23 | Who is CMS-Agent on the tenant | CMS-Agent: `<CLIENT>_MCP_TOKEN` from Secret Manager. platform: a bearer is either a verified agent key (attributed) or the shared `MCP_HTTP_AUTH_TOKEN` (`unattributed-agent` + label). | Which kind of credential the CMS-Agent secret is cannot be read from either repo. | UNKNOWN | SYSTEM-SECURITY-BOUNDARIES.md §3 carries the UNKNOWN. |
| L-24 | Chat tool bound | platform audit: `CMS_AGENT_BOUNDS.maxTools = 96`; CMS-Agent `MAX_CONVERSATION_TOOLS = 99`. | At the pins both are 99 (#694 raised platform's). | VERIFIED-BOTH-SIDES (resolved) | Registry row; the drift gate compares the two literals. |
| L-25 | Tenant chat bearer scope | CMS-Agent: 11 tools locked by `docs/site-credential-scope-lock.json`. platform: the chat only reaches "workspace-orchestration ones … plus `visual_identity_propose`". | Platform's call list (`agent/tools.ts:818-1360`, `brand-imagery-proxy.ts:518-533`, `publication-outputs.ts:57`) uses `agent_resolve`, `agent_converse`, `workspace_get_nodes`, `workflow_start_dry_run`, `workflow_run_all`, `workflow_get_run`, `workflow_get_run_cost`, `workflow_publish_readiness`, `workflow_publish_run`, `workflow_set_operator_publish_decision`, `visual_identity_propose` — and **nine tools that are not in the 11-tool scope**: `node_get_latest_output` (`publication-outputs.ts:57`), `workflow_retry_node`, `workflow_set_node_budget_override`, `workspace_update_node_model_config` (`requests/budget-override.ts:47-60`), `workflow_cancel_run` (`admin-requests.ts:107`), and the Insights tab's `feedback_list`, `playbook_get`, `optimizer_status`, `learning_list_observations` (`analytics-insights.ts`, since #694). | CONTRADICTED | S-07: these calls are refused by CMS-Agent's scope check; the reconciler re-narrows a hand-widened bearer daily. Separately, the scope check does not bind run-addressed calls to the bearer's project (S-26). |

## Disposition summary

- Resolved from code, carried into the system docs: L-01, L-02, L-03, L-04, L-06, L-07, L-08, L-09, L-10, L-11, L-12, L-14, L-19, L-20, L-21, L-22, L-24, L-25.
- Documentation-only drift (no code change implied): L-05, L-17, L-18.
- Left UNKNOWN because no repository can settle it: L-09 (production dims rows), L-23 (credential kind). L-15 moved from UNKNOWN to CONTRADICTED when #694 merged (S-23).
- Pins moved during the synthesis (CMS-Agent #267, platform #694, pdf-tool #78); every row above was re-read against the new pins — the per-row effect is recorded in SYSTEM-OPERATIONS.md §6.1. L-08 was **not** resolved by CMS-Agent #267 (its new §9 repeats the direct PDF-Tool edge).
